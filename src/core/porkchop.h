// Stub: porkchop.h
// In tembed-oink the original Porkchop orchestrator is replaced by src/app/.
// oink.cpp #includes this header but never references the Porkchop class, so an
// empty stub is sufficient to satisfy the include.
#pragma once
